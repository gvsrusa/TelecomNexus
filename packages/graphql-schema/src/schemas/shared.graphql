scalar DateTime

type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}

type Address {
  street: String!
  city: String!
  state: String!
  zip: String!
  country: String!
}
